# Project State -- CareerLens
> Last updated: 2026-03-19
> Last session: make-it (initial build)

## Current Status
App is running locally with all services healthy. Build-verify passed -- login, roles,
permissions, pages, API, seed data, mock services, and logout all verified.

## Build Completed
- Phase 0: Preflight -- PASSED
- Phase 1: Ideation -- COMPLETE
- Phase 2: Design -- COMPLETE
- Phase 3: Build -- COMPLETE
- Phase 4: Ship -- PENDING

## What Was Built
- Pages: Dashboard, Profile, Jobs, Applications, Agents (6 AI agents), Settings, Admin (Users, Roles)
- API endpoints: /api/auth (login, callback, me, logout), /api/dashboard, /api/profile (CRUD + skills, experience, education), /api/jobs (CRUD + analyze), /api/applications (CRUD + status), /api/agents (conversations, messages per agent), /api/settings, /api/users (admin), /api/roles (admin), /api/permissions (admin)
- Auth: OIDC via mock-oidc (local dev), stateless JWT httpOnly cookies
- Roles: Super Admin (25 perms), Admin (21 perms), Pro User (16 perms), User (5 perms)
- AI features: 6 agents (Scout, Tailor, Coach, Strategist, Brand Advisor, Coordinator) with model tier assignments (Heavy/Standard/Light), AI provider abstraction (Anthropic Foundry, Anthropic Direct, OpenAI, Ollama)
- Infrastructure: Docker Compose with 4 services (frontend, backend, db, mock-oidc)
- Mock services: mock-oidc on port 10190

## Build-Verify Results
- Auth flow: PASSED (all 4 roles login with correct permissions from database)
- API endpoints: All returning data through frontend proxy
- Pages: All 6+ pages loading (200)
- Permission boundaries: PASSED (Pro User gets 403 on users/roles, User gets 403 on admin)
- Logout: PASSED (POST clears cookie, subsequent requests get 401)
- Mock services: mock-oidc seeded with 4 test users

## Tech Stack
- Backend: FastAPI + SQLAlchemy + Alembic + PostgreSQL 16
- Frontend: Next.js 15 + TypeScript + Tailwind CSS
- Auth: OIDC + JWT (httpOnly cookies)
- AI: Multi-provider abstraction (Anthropic Foundry default)
- Infra: Docker Compose

## Ports
- Frontend: 3300
- Backend: 8300
- Database: 5600
- Mock-OIDC: 10190

## Test Users
- mock-admin / admin@career-lens.local -> Super Admin
- mock-manager / manager@career-lens.local -> Admin
- mock-pro / pro@career-lens.local -> Pro User
- mock-user / user@career-lens.local -> User

## Known Issues
- Agent chat uses placeholder AI responses (real AI integration requires API keys)
- Job analyze endpoint is a placeholder
- Resume upload endpoint is a placeholder

## Next Steps
- Run /ship-it to deploy
- Run /resume-it to continue development
- Configure AI_PROVIDER and API keys for real AI agent responses
