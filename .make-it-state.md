# Project State -- CareerLens
> Last updated: 2026-03-19
> Last session: resume-it

## Current Status
App is running locally with all services healthy. AI agents wired to real provider calls
(graceful fallback when no API key configured). Tier 2 prompt management system fully
operational with admin UI, versioning, audit logging, and save/test/publish workflow.

## Build Completed
- Phase 0: Preflight -- PASSED
- Phase 1: Ideation -- COMPLETE
- Phase 2: Design -- COMPLETE
- Phase 3: Build -- COMPLETE
- Phase 4: Ship -- PENDING

## What Was Built
- Pages: Dashboard, Profile, Jobs, Applications, Agents (6 AI agents), Settings, Admin (Users, Roles, Prompts)
- API endpoints: /api/auth (login, callback, me, logout), /api/dashboard, /api/profile (CRUD + skills, experience, education), /api/jobs (CRUD + analyze), /api/applications (CRUD + status), /api/agents (conversations, messages per agent), /api/settings, /api/users (admin), /api/roles (admin), /api/permissions (admin), /api/admin/prompts (list, detail, update with save/test/publish)
- Auth: OIDC via mock-oidc (local dev), stateless JWT httpOnly cookies
- Roles: Super Admin (27 perms), Admin (23 perms), Pro User (16 perms), User (5 perms)
- AI features: 6 agents with real AI provider calls, managed prompts with versioning/audit, safety preamble, prompt validation, adversarial testing, runtime caching
- Infrastructure: Docker Compose with 4 services (frontend, backend, db, mock-oidc)
- Mock services: mock-oidc on port 10190

## Recent Changes (resume-it session)
- Wired AI agent responses through provider abstraction (replaces placeholder)
- Built Tier 2 prompt management: DB tables, versioning, audit log, admin API, admin UI
- Added safety preamble, prompt validation, adversarial testing
- Added 6 seeded system prompts with version 1
- Added prompts RBAC permissions (view/edit)
- Added "general" context type for conversations

## Build-Verify Results
- Auth flow: PASSED (all 4 roles login with correct permissions from database)
- API endpoints: All returning data through frontend proxy
- Pages: All 9 pages loading (200) including /admin/prompts
- Permission boundaries: PASSED (27 permissions for Super Admin)
- Logout: PASSED (POST clears cookie, subsequent requests get 401)
- Mock services: mock-oidc seeded with 4 test users
- Prompts API: All 6 agent prompts accessible

## Tech Stack
- Backend: FastAPI + SQLAlchemy + Alembic + PostgreSQL 16
- Frontend: Next.js 15 + TypeScript + Tailwind CSS
- Auth: OIDC + JWT (httpOnly cookies)
- AI: Multi-provider abstraction (Anthropic Foundry default)
- Infra: Docker Compose

## Ports
- Frontend: 3300
- Backend: 8300
- Database: 5600
- Mock-OIDC: 10190

## Test Users
- mock-admin / admin@career-lens.local -> Super Admin
- mock-manager / manager@career-lens.local -> Admin
- mock-pro / pro@career-lens.local -> Pro User
- mock-user / user@career-lens.local -> User

## Known Issues
- Agent chat returns graceful fallback when no AI API key is configured
- Job analyze endpoint is a placeholder
- Resume upload endpoint is a placeholder

## Next Steps
- Configure AI provider API key (ANTHROPIC_API_KEY, OPENAI_API_KEY, or AZURE_AI_FOUNDRY_*) for real AI responses
- Implement Playwright web scraping for job listing URLs
- Implement resume PDF/Word upload and parsing
- Run /ship-it to deploy
