apiVersion: v1
kind: Service
metadata:
  name: kokoro-tts
  namespace: careerlens
  labels:
    app: kokoro-tts
    component: tts
    part-of: careerlens
spec:
  type: ClusterIP
  selector:
    app: kokoro-tts
  ports:
    - name: http
      port: 8880
      targetPort: 8880
      protocol: TCP
