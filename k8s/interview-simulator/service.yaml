apiVersion: v1
kind: Service
metadata:
  name: interview-simulator
  namespace: careerlens
  labels:
    app: interview-simulator
    component: simulator
    part-of: careerlens
spec:
  type: ClusterIP
  selector:
    app: interview-simulator
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      protocol: TCP
